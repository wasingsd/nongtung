# คู่มือการติดตั้ง Google Tag Manager (GTM) สำหรับ Nongtung

คู่มือนี้จะช่วยคุณตั้งค่า GTM ให้รับข้อมูลจาก "โค้ดระบบติดตามอัตโนมัติ" ที่ผมได้ติดตั้งไว้ในเว็บไซต์แล้ว โดยจะแยกเป็นแต่ละกรณี (Use Cases) เพื่อให้คุณนำไปตั้งค่าในหน้าจอ GTM ได้ง่ายที่สุดครับ

---

## ส่วนที่ 1: การเตรียมตัว (ตัวแปรพื้นฐาน)
ก่อนเริ่มสร้างแท็ก ให้สร้างตัวแปรเพื่อเก็บรหัส Google Analytics ของคุณก่อน จะได้ไม่ต้องพิมพ์ซ้ำบ่อยๆ

1.  ไปที่เมนู **ตัวแปร** (Variables) > **ตัวแปรที่กำหนดโดยผู้ใช้** (User-Defined Variables) กด **ใหม่** (New)
2.  เลือกประเภท: **ค่าคงที่** (Constant)
3.  ค่า: ใส่รหัส **Measurement ID** (เช่น `G-XXXXXXX`) จาก GA4 ของคุณ
4.  ตั้งชื่อตัวแปร: `GA4_ID`
5.  กด **บันทึก**

---

## ส่วนที่ 1.1: สร้าง "Google แท็ก" (Master Tag) - สำคัญมาก!
ก่อนจะทำเคสอื่นๆ คุณต้องสร้างแท็กหลักนี้เพื่อให้ GTM เชื่อมต่อกับ Google Analytics ได้สำเร็จ (เพื่อแก้คำเตือน "ไม่พบแท็ก Google")

1. ไปที่ **แท็ก** (Tags) > **ใหม่** (New)
2. เลือกประเภท: **แท็ก Google** (Google Tag)
3. รหัสแท็ก (Tag ID): ใส่รหัส `{{GA4_ID}}` ที่เราสร้างไว้
4. การเปิดใช้งาน (Triggering): เลือก **Initialization - All Pages** (หรือ All Pages)
5. ตั้งชื่อแท็ก: `Google Tag - Master`
6. กด **บันทึก**

---

## ส่วนที่ 2: ตั้งค่าตามแต่ละกรณี (Case by Case)

### เคสที่ 1: การติดตามการติดต่อ (Leads)
*ใช้สำหรับ: คนคลิกปุ่ม Contact Us, คลิกเบอร์โทร, คลิกอีเมล หรือคลิกแชท*

**ขั้นตอนที่ 1: สร้าง Trigger (ตัวจุดชนวน)**
1. ไปที่ **ตัวจุดชนวน** (Triggers) > **ใหม่** (New)
2. เลือกประเภท: **เหตุการณ์ที่กำหนดเอง** (Custom Event)
3. ชื่อเหตุการณ์: พิมพ์ว่า `generate_lead` (ต้องสะกดตามนี้เป๊ะๆ)
4. ตั้งชื่อ Trigger: `Event - Generate Lead`
5. กด **บันทึก**

**ขั้นตอนที่ 2: สร้าง Tag (แท็ก)**
1. ไปที่ **แท็ก** (Tags) > **ใหม่** (New)
2. เลือกประเภท: **Google Analytics: เหตุการณ์ GA4**
3. รหัสการวัด: เลือกตัวแปร `{{GA4_ID}}` ที่เราสร้างไว้
4. ชื่อเหตุการณ์: พิมพ์ว่า `generate_lead`
5. การเปิดใช้งาน (Triggering): เลือก Trigger `Event - Generate Lead` ที่เพิ่งสร้าง
6. ตั้งชื่อ Tag: `GA4 Event - Generate Lead`
7. กด **บันทึก**

---

### เคสที่ 2: การติดตามการจองทริปและเช่ารถ (Booking Intent)
*ใช้สำหรับ: คนคลิกปุ่ม Book a Trip หรือ Rent a Van*

**ขั้นตอนที่ 1: สร้าง Trigger**
1. ไปที่ **ตัวจุดชนวน** > **ใหม่**
2. เลือกประเภท: **เหตุการณ์ที่กำหนดเอง**
3. ชื่อเหตุการณ์: พิมพ์ว่า `booking_intent`
4. ตั้งชื่อ Trigger: `Event - Booking Intent`
5. กด **บันทึก**

**ขั้นตอนที่ 2: สร้าง Tag**
1. ไปที่ **แท็ก** > **ใหม่**
2. เลือกประเภท: **Google Analytics: เหตุการณ์ GA4**
3. รหัสการวัด: เลือก `{{GA4_ID}}`
4. ชื่อเหตุการณ์: พิมพ์ว่า `booking_intent`
5. การเปิดใช้งาน (Triggering): เลือก `Event - Booking Intent`
6. ตั้งชื่อ Tag: `GA4 Event - Booking Intent`
7. กด **บันทึก**

---

### เคสที่ 3: การติดตามการคลิกโซเชียลมีเดีย (Social Clicks)
*ใช้สำหรับ: ดูว่าคนส่วนใหญ่ไป Facebook, Instagram หรือ TikTok*

**ขั้นตอนที่ 1: สร้าง Trigger**
1. ชื่อเหตุการณ์: พิมพ์ว่า `click_social`
2. ตั้งชื่อ Trigger: `Event - Social Click`

**ขั้นตอนที่ 2: สร้าง Tag**
1. ชื่อเหตุการณ์: พิมพ์ว่า `click_social`
2. การเปิดใช้งาน: เลือก `Event - Social Click`
3. ตั้งชื่อ Tag: `GA4 Event - Social Click`

---

### เคสที่ 4: การสมัครรับข่าวสาร (Newsletter)
*ใช้สำหรับ: คนคลิกปุ่ม GO ในส่วนท้ายของเว็บ*

**ขั้นตอนที่ 1: สร้าง Trigger**
1. ชื่อเหตุการณ์: พิมพ์ว่า `newsletter_signup`
2. ตั้งชื่อ Trigger: `Event - Newsletter Signup`

**ขั้นตอนที่ 2: สร้าง Tag**
1. ชื่อเหตุการณ์: พิมพ์ว่า `newsletter_signup`
2. การเปิดใช้งาน: เลือก `Event - Newsletter Signup`
3. ตั้งชื่อ Tag: `GA4 Event - Newsletter Signup`

---

## ส่วนที่ 3: วิธีการตรวจสอบ (Debugging & Verification)

หลังจากตั้งค่าทริกเกอร์และแท็กเสร็จแล้ว เราต้องตรวจสอบว่าข้อมูลถูกส่งไปจริงๆ หรือไม่ โดยแบ่งเป็น 2 ระดับครับ:

### ระดับที่ 1: ตรวจสอบผ่าน GTM Preview (ก่อนการเผยแพร่)
วิธีนี้จะบอกว่า GTM "ได้ยิน" เหตุการณ์ที่คุณต้องการหรือไม่

1.  ที่หน้าหลักของ GTM กดปุ่ม **แสดงตัวอย่าง (Preview)** ที่มุมขวาบน
2.  ใส่ URL เว็บไซต์ของคุณในหน้าต่าง Tag Assistant แล้วกด **Connect**
3.  เว็บไซต์จะเปิดขึ้นมาในหน้าต่างใหม่ (ให้เปิดทิ้งไว้)
4.  สลับจอกลับไปที่หน้าต่าง **Tag Assistant** (หน้าที่มีพื้นหลังสีน้ำเงิน) คุณจะเห็นผลการทำงานดังนี้:
    - **ฝั่งซ้าย (Summary)**: จะมีรายการเหตุการณ์ขึ้นมา เช่น `initialization`, `container loaded`, `generate_lead`
    - **ฝั่งขวา (Tags Fired)**: เป็นรายการแท็กที่ทำงานสำเร็จ
5.  **วิธีทดสอบจริง**: 
    - ไปที่หน้าเว็บไซต์ที่เปิดไว้ ลองกดปุ่ม **"Contact Us"** หรือ **"Book a Trip"**
    - สลับจอกลับมาดูที่ Tag Assistant คุณต้องเห็นเหตุการณ์ `generate_lead` หรือ `booking_intent` ปรากฏขึ้นที่แถบด้านซ้าย
    - และเห็นชื่อแท็ก (เช่น `GA4 Event - Generate Lead`) ย้ายขึ้นไปอยู่ในส่วน **Tags Fired**

---

### ระดับที่ 2: ตรวจสอบผ่าน GA4 Real-time (ยืนยันว่าข้อมูลถึง Google แล้ว)
แม้ GTM จะ Tag Fired แล้ว แต่เราควรไปดูในหลังบ้านของ Google Analytics ด้วย

1.  ไปที่หน้า **Google Analytics 4** ของคุณ
2.  ไปที่เมนู **รายงาน (Reports)** > **เรียลไทม์ (Real-time)**
3.  เลื่อนหน้าจอลงมาดูการ์ดที่ชื่อว่า **"จำนวนเหตุการณ์ตามชื่อเหตุการณ์" (Event count by Event name)**
4.  เมื่อคุณกดปุ่มในเว็บไซต์ (ขณะที่เปิด Debug mode อยู่) คุณควรจะเห็นชื่อ `generate_lead` หรือ `booking_intent` ปรากฏขึ้นในกราฟเรียลไทม์ภายใน 10-20 วินาที

---

### ขั้นตอนสุดท้าย: การเผยแพร่ (Publish)
ถ้าคุณทดสอบทั้ง 2 ระดับแล้วเห็นข้อมูลขึ้นตามปกติ ให้กลับไปที่หน้าหลักของ GTM แล้วทำดังนี้:

1.  กดปุ่ม **ส่ง (Submit)** ที่มุมขวาบน
2.  ตั้งชื่อเวอร์ชัน (เช่น `Added lead and booking tracking`)
3.  กด **ประกาศใช้ (Publish)**

---

---

> [!NOTE]
> **ข้อมูลเพิ่มเติม**: ในโค้ดที่ผมเขียนไว้ ผมแถมการส่งข้อมูลไปที่ **Facebook Pixel** ให้ด้วยอัตโนมัติ (เป็นเหตุการณ์ Lead และ InitiateCheckout) ถ้าคุณติดตั้ง Facebook Pixel ไว้ใน GTM ข้อมูลจะไหลเข้าทั้งสองที่พร้อมกันเลยครับ!

---

## ส่วนที่ 4: ปัญหาที่พบบ่อย (Troubleshooting)

หากคุณทดสอบแล้ว "ไม่ติด" หรือ "แท็กไม่ทำงาน" ให้เช็ค 3 จุดนี้ครับ:

### 1. เช็คว่า Deploy โค้ดหรือยัง?
**วิธีเช็คที่ชัวร์ที่สุด:** ที่หน้าเว็บของคุณ (เว็บจริง) ให้กด **F12** > เลือกแถบ **Console** แล้วลองกดปุ่มจองทริป 
- **ถ้าเห็นข้อความสีๆ** `🚀 [Nongtung Tracking] ...` แสดงว่าโค้ดใหม่ทำงานแล้ว
- **ถ้าไม่เห็นอะไรเลย** แสดงว่าโค้ดที่ผมแก้ยังไม่ได้ถูกอัปโหลดขึ้นไป ให้รันไฟล์ `git_deploy.bat` อีกรอบครับ

### 2. พิมพ์ชื่อเหตุการณ์ (Event Name) ใน GTM ผิด?
ใน GTM ช่องชื่อเหตุการณ์ต้องเป็นภาษาอังกฤษ และพิมพ์ให้ตรงตามนี้เป๊ะๆ (ไม่มีเว้นวรรค และเป็นตัวพิมพ์เล็กทั้งหมด):
- `generate_lead`
- `booking_intent`
- `click_social`
- `newsletter_signup`

### 3. ลืมสร้างหรือตั้งค่า "Google แท็ก" (Master Tag)?
หากแท็กหลัก (Master Tag) ในส่วนที่ 1.1 ยังตั้งค่าไม่สำเร็จ หรือลืมใส่ Trigger เป็น **All Pages/Initialization** แท็กอื่นๆ (แท็กลูก) จะไม่สามารถส่งข้อมูลได้ครับ

### 4. GTM Preview ไม่รีเฟรช?
บางครั้งหน้า Preview ของ GTM จะจำค่าเก่า ให้คุณกดปุ่ม **"Refresh"** ที่หน้าต่าง Tag Assistant (หน้าน้ำเงิน) แล้วลองกดปุ่มในเว็บใหม่อีกครั้งครับ

