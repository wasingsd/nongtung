# NONGTUNG UI Style Guide

เอกสารฉบับนี้สรุปรายละเอียดสไตล์ UI และ Design System ที่ใช้ในโปรเจกต์ **NONGTUNG** เพื่อความเป็นเอกภาพในการพัฒนา

---

## 🎨 1. Color Palette (สี)

เราใช้โทนสีที่สื่อถึงธรรมชาติ (Northern Nature) ผสมผสานกับความทันสมัย (Premium Adventure)

### Core Colors
| Name | Hex Code | Usage |
| :--- | :--- | :--- |
| **Primary** | `#f07d3a` | สีหลัก, ปุ่ม Action, เน้นจุดสำคัญ (Orange) |
| **Primary Deep** | `#d76627` | สี Primary เมื่อ Hover หรือเน้นเข้ม |
| **Forest (Dark)** | `#2e3022` | สีพื้นหลังหลัก (เข้ม), ตัวอักษรหลัก, Footer |
| **Forest Light** | `#4c523d` | สีรองในโทนเขียวเข้ม |
| **Accent (Olive)** | `#778950` | สีตกแต่ง, ไอคอนเสริม |
| **Surface** | `#f9f9f5` | สีพื้นหลังส่วน Section, Card (Off-white) |
| **Background** | `#fdfdfb` | สีพื้นหลังรวมของหน้าเว็บ |
| **Ochre** | `#857744` | สีทองดินแดง สำหรับตกแต่ง |

---

## Typography (ตัวอักษร)

เน้นการผสมผสานระหว่าง Font ที่มีความทันสมัยและอ่านง่าย

- **Heading (หัวข้อ):** `Urbanist` (sans-serif)
    - เน้นตัวหนาพิเศษ (**Black/ExtraBold**)
    - ใช้ `tracking-tighter` เพื่อให้ดูพรีเมียม
    - บ่อยครั้งจะใช้ตัวเอียง (`italic`) ในบางคำเพื่อสร้าง Dynamic
- **Body (เนื้อหา):** `Kanit` (sans-serif)
    - รองรับภาษาไทยได้สมบูรณ์แบบ
    - ใช้ `font-medium` หรือ `font-bold` สำหรับเนื้อหาทั่วไป

---

## 💎 3. Design Tokens & Components

### 📐 Shadows & Borders
- **Shadow:** ใช้ `.immersive-shadow` (เงาฟุ้ง กระจายตัวกว้าง)
- **Border Radius:** เน้นความโค้งมนขนาดใหญ่เพื่อให้ดูเป็นมิตรแต่หรูหรา
    - `rounded-[2.5rem]` สำหรับ Card
    - `rounded-[3rem]` ถึง `rounded-[4rem]` สำหรับ Section ใหญ่
    - `rounded-full` สำหรับปุ่ม (Pill shape)

### ✨ Visual Effects
- **Glassmorphism:** `.glass-nav` (bg-white/80 + backdrop-blur) ใช้กับ Navbar
- **Animations:** `.fade-in` (เคลื่อนที่ขึ้นเล็กน้อยพร้อมความโปร่งใส)
- **Gradient:** มักใช้ `bg-gradient-to-t` จาก `black/60` ไป `transparent` บนรูปภาพเพื่อให้อ่าน Text ได้ง่าย

### 🔘 Buttons
- **Primary Button:** พื้นหลังสี `primary`, ตัวอักษรสีขาว, ตัวหนา, uppercase, tracking กว้าง
- **Secondary Button:** พื้นหลังโปร่งใส หรือโปร่งแสง (`white/10`) พร้อม border บางๆ

---

## 📐 4. Section Layout & Structure

- **Section Background:** สลับระหว่างสีขาว (`bg-white`) และสีเข้ม (`bg-forest`) เพื่อสร้างจังหวะ (Rhythm) ให้หน้าเว็บ
- **Spacing:** ใช้ `py-24` หรือ `py-32` สำหรับระยะห่างระหว่าง Section เพื่อให้ดูโปร่ง (Breatheable)
- **Grid:** ใช้ Responsive Grid (1 column บน mobile, 3-4 columns บน desktop)
- **Subtitles:** มักใช้ตัวอักษรขนาดเล็กมาก (`text-[10px]`), หนาที่สุด (`font-black`), พิมพ์ใหญ่ทั้งหมด (`uppercase`), และเว้นระยะห่างตัวอักษรมาก (`tracking-[0.4em]`)

---

## 🛠 5. Implementation (Tailwind v4)

กำหนดค่าไว้ใน `@theme` ภายใน `globals.css`:

```css
@theme {
    --color-primary: #f07d3a;
    --color-primary-deep: #d76627;
    --color-accent: #778950;
    --color-forest: #2e3022;
    --color-forest-light: #4c523d;
    --color-ochre: #857744;
    --color-surface: #f9f9f5;
    
    --font-heading: var(--font-urbanist), sans-serif;
    --font-body: var(--font-kanit), sans-serif;
}
```
